# @openrill/sandbox-docker

Docker CLI backend for OpenRill execution handles.

STEP015A provides deterministic command planning, lifecycle ownership, exact label-scoped pruning, and injected CLI tests. Real Docker daemon acceptance is deferred to STEP015B.
