# @openrill/tools-files

Closed workspace Tool catalogue: `workspace.list`, `workspace.stat`, `workspace.read`, `workspace.search`, `workspace.write`, and `workspace.patch`.

Mutations require an expected SHA-256 revision (`MISSING` for creation), use same-directory atomic replacement, and emit bounded diff plus durable Artifact metadata when an Artifact store is configured.
