# @openrill/tool-discovery

Structured Tool Search inspired by the audited OpenClaw source. Large tool catalogs are hidden behind bounded `tool.search`, `tool.describe`, and `tool.call` controls while a small practical core and active Skill-required tools remain directly visible.
