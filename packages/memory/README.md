# @openrill/memory

Profile-persistent, workspace-isolated memory records with explicit provenance, FTS5 lexical search, bounded reads, idempotent remember, explicit forget, and sensitive-content rejection.

The first implementation intentionally does not claim vector embeddings, automatic extraction, hidden memory, or cross-workspace recall.
