# @openrill/agent-kernel

Provider-neutral model/tool loop. It owns budgets, retry boundaries, cancellation, duplicate tool-call protection and ledger projection, but does not import HTTP, WebSocket or SQLite implementations.
