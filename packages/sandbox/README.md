# @openrill/sandbox

Provider-neutral execution backend contract, workspace authority, confinement proof, explicit host fallback policy, and Host backend.

`HOST` never reports `sandboxed=true`. Docker-specific lifecycle is owned by `@openrill/sandbox-docker`.
