# STEP014DR3 Failure Prevention Audit

- exact Windows failure hash maps to `tool not found: `;
- captured parallel SSE fixture uses `call_id` on output items and `item_id` on argument events;
- two provider calls yield exactly two canonical Tool calls, not four;
- terminal emission rejects an empty name;
- conflicting item/call identity bindings reject as `MODEL_STREAM_INVALID`;
- diagnostics show Tool event name/id only and never Tool arguments/results;
- schema 14 and all STEP014 delegation surfaces remain unchanged.
