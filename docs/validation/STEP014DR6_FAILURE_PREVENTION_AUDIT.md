# STEP014DR6 failure prevention audit

- External-model live acceptance owns provider compatibility, two direct children, durable waits, and parent resume.
- Deterministic nested Control UI live acceptance owns persisted depth-2 graph, Protocol projection, actual Chromium tree/detail rendering, and orphan-free shutdown.
- No acceptance assertion depends on a stochastic child choosing `agent.spawn`.
