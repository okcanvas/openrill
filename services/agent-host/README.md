# @openrill/host

Foreground local Host lifecycle, profile single-instance lock, authenticated loopback control endpoint, readiness and graceful shutdown을 소유한다.

STEP002는 DB, Agent, Tool, WebSocket business protocol을 열지 않는다.
